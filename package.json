{
  "name": "web-teleop-robot",
  "version": "1.0.0",
  "description": "A comprehensive web-based teleoperation system for robots",
  "private": true,
  "workspaces": [
    "shared",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:shared\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:shared": "cd shared && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "build:shared": "cd shared && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd shared && npm install && cd ../backend && npm install && cd ../frontend && npm install",
    "clean": "npm run clean:shared && npm run clean:backend && npm run clean:frontend",
    "clean:shared": "cd shared && npm run clean",
    "clean:backend": "cd backend && npm run clean",
    "clean:frontend": "cd frontend && rm -rf .next",
    "test": "npm run test:shared && npm run test:backend",
    "test:shared": "cd shared && npm test",
    "test:backend": "cd backend && npm test",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "keywords": [
    "teleop",
    "robot",
    "ros2",
    "zenoh",
    "web",
    "typescript",
    "nextjs",
    "nodejs",
    "fastapi"
  ],
  "author": "Web Teleop Robot Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/web-teleop-robot.git"
  }
}